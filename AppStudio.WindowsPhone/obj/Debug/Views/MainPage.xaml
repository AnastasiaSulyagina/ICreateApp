<Page
    x:Class="AppStudio.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppStudio.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <Hub x:Name="Container" Grid.Row="1" Background="{StaticResource AppBarBackground}" Margin="0,27,0,-34.666"
             DataContext="{Binding}" HeaderTemplate="{StaticResource PageHeader}">
            <HubSection x:Name="MenuSection" Width="440" Header="меню"
                        HeaderTemplate="{StaticResource HubSectionHeaderTemplate}"/>
            <HubSection x:Name="EventsSection" Width="440" Header="список событий"
                        HeaderTemplate="{StaticResource HubSectionHeaderTemplate}">
                <DataTemplate>
                    <ListView x:Name="eventList" HorizontalAlignment="Left"
                  Height="592" Margin="10,95,0,0" VerticalAlignment="Top" Width="460"
                  ItemsSource="{Binding Events}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <!--<StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding User.UserName}" Width="50"/>
                                    <TextBlock Text="{Binding Description}"/>
                                </StackPanel>-->
                                <StackPanel Margin="0,0,0,5" Background="White" Height="150">
                                    <TextBlock Text="{Binding User.UserId}" TextWrapping="Wrap" Margin="5,5,5,5" Foreground="#FF21201E" />
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="5,5,5,5" Foreground="#FF21201E"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                </DataTemplate>
                
                
            </HubSection>
            <HubSection x:Name="MapSection" Width="440" Header="карта"
                        HeaderTemplate="{StaticResource HubSectionHeaderTemplate}"/>
        </Hub>
    </Grid>
</Page>

